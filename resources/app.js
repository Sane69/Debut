// Require CSS file
require('./scss/app.scss');

// Require JS files
// require('./js/template/product.js');

/* (() => { */
// const axios = require('axios');
/*
const $ = require('jquery');

const Swal = require('sweetalert2');

$('[name="add"]')
  .click((event) => {
    event.preventDefault();
    Swal.fire('Hello world!');
  });
*/

/* }).apply(this); */
